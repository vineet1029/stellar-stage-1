import * as React from 'react';
import { StyleSheet, Text } from 'react-native';

export default class DailyPic extends React.Component{
    render() {
        return(
            <Text style={StyleSheet.flatten}>Daily Pictures Screen!!</Text>
        );
    }
}